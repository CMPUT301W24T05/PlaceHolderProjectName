package ca.cmput301t05.placeholder.notifications;

public enum MilestoneType {
    FIRSTATTENDEE, //first person has checked in
    FIRSTSIGNUP, //first person has signed up
    HALFWAY, //half of full capacity
    FULLCAPACITY, //reached full capacity
}
